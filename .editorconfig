 # 表示当前是项目根目录
root = true

# 所有文件都使用配置
[*]
# 编码格式
charset = utf-8
# Tab键缩进的样式，由space和table两种 一般代码中是space
indent_style = space
# 缩进size为2
indent_size = 2
# 以lf换行 默认win为crlf mac和linux为lf
end_of_line = lf
# 末尾加一行空行
insert_final_newline = true
#  去掉行尾多余空格
trim_trailing_whitespace = true
